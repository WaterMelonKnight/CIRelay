# @cirelay/mcp

The local stdio MCP transport for CIRelay. After publication, run it with `npx @cirelay/mcp` and provide `GITHUB_TOKEN`.

See the [CIRelay repository](https://github.com/WaterMelonKnight/CIRelay#readme) for installation, usage, security, and license information.
